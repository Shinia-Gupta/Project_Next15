//This file is used to render the modal content when the route is first loaded
//So we don't have to render anything on the modal on first route loading, hence, we return null
export default function Default(){
    return null;
}